# --- Core (required) ---
streamlit>=1.39
numpy>=2.1
scikit-learn>=1.5.2
pandas>=2.2.3

# --- Semantic embedding layer (Sentence-BERT) ---
# Lazy-imported in embedding.py; without it the app falls back to a TF-IDF
# cosine semantic score. Pulls in PyTorch, which has wheels for:
#   - Linux x86_64 (Streamlit Community Cloud — pinned to py3.11 via runtime.txt)
#   - macOS arm64 (Apple Silicon)
#   - Windows x86_64
# NOT available for Intel macOS + Python 3.13/3.14 — comment this out if you
# hit a torch resolution error on that combination.
sentence-transformers>=3.2.1

# --- Optional file readers (only needed if users upload PDF / DOCX) ---
pdfplumber>=0.11.4
python-docx>=1.1.2

# --- Optional: fuzzy skill matching in taxonomy.py ---
rapidfuzz>=3.10
